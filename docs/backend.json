{
  "entities": {
    "Device": {
      "title": "Network Device",
      "description": "A representation of a network hardware node (Router, GPON, etc.)",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "ipAddress": { "type": "string" },
        "status": { "type": "string", "enum": ["ONLINE", "OFFLINE"] },
        "manufacturer": { "type": "string" },
        "model": { "type": "string", "description": "The specific model number of the hardware device." },
        "firmware": { "type": "string" },
        "mac": { "type": "string" },
        "ssid": { "type": "string" },
        "password": { "type": "string", "description": "The Wi-Fi password for the SSID broadcast by this device." },
        "type": { "type": "string", "enum": ["router", "extender", "gpon"] }
      },
      "required": ["name", "ipAddress", "status", "type"]
    }
  },
  "auth": {
    "providers": ["google.com", "password"]
  },
  "firestore": {
    "/users/{userId}/devices/{deviceId}": {
      "schema": "Device",
      "description": "User's managed network devices."
    }
  }
}
